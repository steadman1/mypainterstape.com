
function CTADownloadURLWithNext({ works, workIndex, setWorkIndex, detailIndex, setDetailIndex }: { url: string | undefined, primaryColor: Color; secondaryColor: Color; workIndex: number, setWorkIndex: React.Dispatch<React.SetStateAction<number>>, detailIndex: number, setDetailIndex: React.Dispatch<React.SetStateAction<number>> }) {
    return (
        <div>
        
        </div>
    );
}

export default CTADownloadURLWithNext;